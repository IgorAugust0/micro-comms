/**
 * Differently from the products-api status options, where we only have 'APPROVED' and 'REJECTED',
 * the sales-api also needs a 'PENDING' status for orders that are still being processed.
 */
export const APPROVED: string = "APPROVED";
export const PENDING: string = "PENDING";
export const REJECTED: string = "REJECTED";
